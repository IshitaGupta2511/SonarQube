pipeline {
    agent any

    stages {
        stage('install-docker') {
            steps {
                sh'''
                sudo yum install docker
                sudo systemctl start docker
                sudo systemctl enable docker
                '''
            }
        }
    }
}
